---
const {imageUrl} = Astro.props;

const imageUrlStyle = `url(${imageUrl})`;
---
<style define:vars={{imageUrlStyle}}>
    .bg-image {
        background-image: var(--imageUrlStyle);
    }
</style>
<div
    class="h-full bg-image bg-cover bg-center bg-fixed"
/>
